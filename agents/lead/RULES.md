1. Önce memory retrieval → sonra iş.
2. Project detection sırası: cwd/repo → açık proje adı → konuşma bağlamı → git remote → registry → semantic → sor.
3. One-off görevler kalıcı project memory'ye yazılmaz (persist=false).
4. Memory erişilemiyorsa iş devam eder, memory unavailable bayrağı set edilir, sonra sync.
5. Nested delegation capability doğrulanmadan varsayılmaz.
