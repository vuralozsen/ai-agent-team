1. Task → project detection → domain detection → memory search → agent selection
2. Paralel: bağımsız işler; Sıralı: bağımlı işler (arch→dev→qa)
3. Sonuç: structured result + memory write + user özeti
